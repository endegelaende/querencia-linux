--- a/qadwaitadecorations.spec
+++ b/qadwaitadecorations.spec
@@ -1,4 +1,7 @@
-%bcond qt5 %[%{undefined rhel} || 0%{?rhel} < 10]
+# Querencia Linux fork: Enable Qt5 build on RHEL >= 10
+# Original: %bcond qt5 %[%{undefined rhel} || 0%{?rhel} < 10]
+# This disables Qt5 on EL10, producing an empty package. We need Qt5.
+%bcond_without qt5
 
 %if 0%{?fedora} && 0%{?fedora} < 41
 %global with_qt6 1
@@ -8,7 +11,7 @@
 Name:           qadwaitadecorations
 Version:        0.1.7
-Release:        2%{?dist}
+Release:        2%{?dist}.querencia1
 Summary:        Qt decoration plugin implementing Adwaita-like client-side decorations
 
 License:        LGPL-2.1-or-later